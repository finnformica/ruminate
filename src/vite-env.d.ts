/// <reference types="vite/client" />

/** The changelog's build stamp, `<newest week>.<hash>` (see vite.config.ts).
 * The dialog after an update compares it with the one it stored last time. */
declare const __CHANGELOG_VERSION__: string
