### Added

- Ruminate tells you what changed after it updates. Open it on a new version and a **What's new** card appears in the corner, listing what has happened since the version this device was last on, a line each, with a way through to the full changelog. It never takes over the screen, goes away on <kbd>Esc</kbd> or a click, and appears once per version.
