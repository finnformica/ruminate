### Changed

- A note follows only downstream links unless you say otherwise. **Show links** in Settings → Editor now starts on **Downstream**, so a note reads as its outline, as it always has; choose **Upstream** or **Both** to see a block's other parents beneath it. It was set to Both when the setting arrived last week.
