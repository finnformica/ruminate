### Changed

- Focusing a block that isn't a heading keeps it as an ordinary row. It used to be flattened into the page title, which lost a to-do's box, a picture, a quote's bar and a code block's lines. Only a heading becomes the title now; everything else leads the view as its first row and reads exactly as it does in the note.
